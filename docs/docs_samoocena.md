# Samoocena - Koniec Tygodnia 1
**Imię i Nazwisko:** [Twoje Imię i Nazwisko]  
**Data:** 29.05.2026  

| Umiejętność | Poziom (1-5) | Komentarz |
| :--- | :---: | :--- |
| **JavaScript (zmienne, funkcje, tablice)** | 4 | Dobrze rozumiem asynchroniczność (`async/await`), operacje na tablicach (`map`, `filter`) oraz destrukturyzację, które były kluczowe w React i Node.js. |
| **Node.js + Express** | 4 | Potrafię postawić serwer, skonfigurować routing i pisać middleware. Rozumiem, jak Express przetwarza żądania i współpracuje z bazą danych. |
| **REST API (GET, POST, PUT, DELETE)** | 5 | Pełne zrozumienie metod HTTP, przesyłania danych w nagłówkach i ciele żądania (JSON) oraz zwracania odpowiednich kodów statusu. |
| **MySQL + SQL** | 4 | Znam podstawy relacyjnych baz danych, potrafię pisać zapytania `SELECT`, `INSERT`, `UPDATE`, `DELETE` oraz modyfikować strukturę tabel za pomocą `ALTER TABLE`. |
| **React (komponenty, stan, efekty)** | 4 | Sprawnie zarządzam stanem (`useState`), efektami ubocznymi (`useEffect`) oraz przepływem danych za pomocą propsów. Znam zasady działania hooków. |
| **Git (commit, branch, merge, PR)** | 4 | Swobodnie pracuję na gałęziach, dbam o opisy commitów. Rozumiem proces tworzenia Pull Requestów i integracji kodu. |
| **Debugowanie** | 5 | Ten tydzień bardzo mocno rozwinął tę umiejętność. Potrafię czytać błędy ESLinta, analizować logi Nodemona w terminalu oraz błędy parsowania JSON. |
| **Struktura projektu** | 4 | Rozumiem podział na architekturę trójwarstwową (Frontend, Backend, Baza danych) oraz separację kodu wewnątrz backendu (controllers, routes, db). |
| **Dokumentacja** | 4 | Potrafię czytać dokumentację techniczną bibliotek oraz samodzielnie opisywać działanie systemu i kroki wdrożeniowe. |
| **Jakość kodu (lint, formatowanie)** | 4 | Wiem, jak działają narzędzia linterów (ESLint) i formaterów (Prettier). Potrafię dostosować kod pod rygorystyczne reguły (np. nieużywane zmienne, puste bloki). |
| **Praca samodzielna** | 4 | Potrafię analitycznie podejść do problemu, szukać rozwiązań komunikatów o błędach i wdrażać poprawki krok po kroku. |
